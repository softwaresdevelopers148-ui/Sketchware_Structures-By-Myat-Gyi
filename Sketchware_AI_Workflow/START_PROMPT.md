# AI Agent project-start prompt

Before working on this Sketchware project, do the following:

1. Read `AGENTS.md`.
2. Read `CONTEXT_MAP.md` and identify the relevant guide files.
3. Read the relevant guide sections only.
4. Inspect the current project and preserve existing IDs, URLs, and behavior unless the user asks for a change.
5. State the implementation plan and files to change.
6. Implement with Sketchware Pro-compatible Java/blocks.
7. Validate JSON, imports, package names, permissions, and View IDs.
8. Report the repository commit, guide files used, tests run, and remaining build steps.

Repository reference:
https://github.com/softwaresdevelopers148-ui/Sketchware_Structures-By-Myat-Gyi

Do not claim that this repository permanently retrains the model. Treat it as versioned project context and rules.
